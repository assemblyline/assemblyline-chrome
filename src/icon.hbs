<svg aria-hidden="true"
     class="octicon {{css}} build-status-icon text-{{state}}"
     width="{{scale width size}}"
     height="{{scale height size}}"
     role="img"
     version="1.1"' 
     viewBox="0 0 {{width}} {{height}}"' >
       <path d="{{ path }}" />
</svg>
